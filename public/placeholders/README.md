# Placeholder Media

Replace these with real assets. All paths are referenced in code with TODO comments.

**Video files (`*.mp4` / `*.MP4`) under this folder are not committed to Git** (they often exceed GitHub’s size limits). Add them locally for development, or rely on YouTube fallbacks in production.

## Required files

- **hero-showreel.mp4** – Silent looping hero showreel (full viewport). Used on Home hero.
- **hero-poster.jpg** – Poster image for hero video (preload/fallback).
- **plates/plate-01.jpg** … **plate-12.jpg** – Abstract gallery plate images (no stock). Used in Portfolio/Case Studies.
- **reels/reel-01.mp4** … **reel-06.mp4** – Short vertical placeholder loops for reels carousel.

If a file is missing, the UI falls back to a styled placeholder div.
